[project]
name = "present"
version = "0.1.0"
description = "Track how long since each U.S. House member last voted"
requires-python = ">=3.12"
dependencies = [
    "pycongress",
]

[tool.uv.sources]
pycongress = { git = "https://github.com/dwillis/PyCongress.git" }
